app = "liberty-brokers"
primary_region = "iad"

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  min_machines_running = 1

[vm]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
